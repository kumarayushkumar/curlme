meta {
  name: Follow
  seq: 5
}

auth {
  mode: inherit
}

docs {
  The follow graph. `targetUsername` has to be an account that exists and is
  not you, so register a second account first or point it at a seeded one.
}
