meta {
  name: Auth
  seq: 2
}

docs {
  Username and password auth, plus the GitHub device flow the CLI uses.

  `register` and `signin` both write `token` into the active environment, so
  every other folder works straight after either one.
}
